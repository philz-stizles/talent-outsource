const sendEmailVerification = async (email: string, token: string) => {};
const sendResetPassword = async (email: string, token: string) => {};

export default { sendEmailVerification, sendResetPassword };
